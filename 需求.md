项目	内容
产品名称	EyeGuard 定时休息护眼软件
文档版本	V1.0
日期	2026-05-28

1. 产品概述
1.1 项目背景
长时间使用电脑容易导致视觉疲劳和身体不适。本软件旨在通过定时提醒和强制休息机制，帮助用户养成良好的用眼习惯，保护视力健康。

1.2 产品目标
开发一款运行于 Windows 桌面的定时休息护眼软件，具备倒计时提醒、屏幕锁定、多屏幕支持、全屏检测、键盘鼠标静止检测等功能，提供灵活可配置的休息策略。

1.3 目标用户
长期使用电脑的办公人员、程序员、设计师、学生等。

2. 运行环境
操作系统：Windows 10 / Windows 11（64位）

多显示器支持：主屏幕 + 扩展屏幕（不同分辨率、缩放比例）

3. 功能需求
3.1 工作状态
3.1.1 倒计时窗口
位置：桌面右上角（默认），用户可拖动移动

尺寸：固定 88×44 像素，不可调整大小

显示内容：离下次休息的剩余时间，格式 MM:SS

刷新频率：每秒更新一次，自动倒计时

背景进度条：随时间减少，从右向左消退，表示时间进度

始终显示：工作状态下始终置顶（根据用户设置决定是否总在最前）

无标题栏按钮：窗口无最大化、最小化、关闭按钮，用户无法直接关闭

3.1.2 右键菜单（点击窗口弹出）
菜单项	行为
立即休息	立即锁定所有屏幕，进入休息状态
推迟休息 3 分钟	在当前倒计时基础上增加 3 分钟，消耗一次推迟次数
推迟休息 5 分钟	在当前倒计时基础上增加 5 分钟，消耗一次推迟次数
推迟休息 10 分钟	在当前倒计时基础上增加 10 分钟，消耗一次推迟次数
总在最前显示	窗口置顶，不被其他窗口遮挡；菜单前方显示 ✓
取消最前显示	窗口可被其他窗口遮挡；菜单前方显示 ✓（对应“总在最前显示”取消 ✓）
设置属性	打开设置窗口（单例模式）
关闭退出	退出整个应用程序
推迟次数限制：

每个工作周期内允许的推迟总次数由“设置属性”中配置（1-6次）。

每次点击推迟菜单消耗一次次数，次数耗尽后菜单项置灰不可用。

下一个休息周期结束后，推迟次数重置为配置值。

3.1.3 3分钟提醒
当倒计时剩余 3 分钟 时：

弹出提示窗口（非模态，可被后续操作关闭），内容：“3 分钟后即将休息”

播放提示音：breakpre.mid（一遍）

若用户解锁屏幕时该提示窗口仍存在，应自动关闭。

3.2 休息状态
3.2.1 进入休息
当工作时间倒计时归零时：

播放提示音：break.mid（一遍）

隐藏工作时的倒计时窗口，停止计时

锁定所有屏幕（主屏+扩展屏）：

背景黑色遮罩，覆盖整个屏幕

屏幕中央显示休息倒计时（格式 MM:SS），每秒刷新

用户无法看到原有屏幕内容，无法进行任何操作

屏蔽以下系统按键：Win 徽标键、Alt+Tab、Alt+F4

3.2.2 休息模式（三种）
模式	行为
非强制	解锁图标始终显示在黑色背景右下角，用户可随时点击解锁
一般强制	休息满 1分钟 后，右下角显示解锁图标，用户点击后解除锁定
完全强制	整个休息过程中无解锁图标，用户不可中途解锁，必须等到休息结束
3.2.3 退出休息
用户主动解锁（仅非强制/一般强制模式下，点击解锁图标）：

解除屏幕锁定

播放提示音：unlock.mid（一遍）

立即开始 一个新的完整工作周期（倒计时 = 工作时间长度）

显示工作倒计时窗口，开始倒计时

休息时间自然结束：

自动解除屏幕锁定

播放提示音：unlock.mid（一遍）

立即开始新的完整工作周期

若在强制休息模式下用户解锁时，之前弹出的“3分钟后休息”提示窗口（若未关闭），应自动关闭。

3.3 设置属性窗口
3.3.1 打开方式
双击系统托盘图标

右键托盘图标 → “设置属性”

工作倒计时窗口右键 → “设置属性”

约束：设置窗口为 单例模式，只能同时打开一个。

3.3.2 窗口特性
模态窗口（Modal）

修改后点击“保存”弹出确认框，确认后立即生效；点击“取消”恢复修改前值；点击关闭图标（X）等同于取消

保存生效后，工作倒计时窗口应立即按新设置开始新周期

3.3.3 配置项列表
配置项	可选值/说明	默认值
开机自启动	勾选/不勾选	不勾选
工作时间长度（分钟）	数值输入，正整数	25
休息时间长度（分钟）	数值输入，正整数	5
允许推迟休息次数	下拉选择 1-6	3
软件全屏运行时暂停计时	勾选启用	不勾选
键盘鼠标静止X分钟后暂停计时	勾选启用 + X数值（3-30）	勾选，X=5
休息模式	下拉：非强制 / 一般强制 / 完全强制	一般强制
说明：

“键盘鼠标静止暂停计时”：勾选后，当用户键盘/鼠标静止超过 X 分钟，工作倒计时暂停；用户恢复操作后，重新开始一个新周期（不是继续）。

“软件全屏运行时暂停计时”：检测到全屏应用（特征：独占屏幕、无标题栏按钮）时暂停计时；恢复后继续（或重新？按需求：暂停时不计时，恢复后继续原有倒计时）。注意排除系统应用（如任务管理器、开始菜单等）。

3.4 系统托盘
图标：程序启动后在系统托盘显示自定义 icon

右键菜单：

设置属性 → 打开设置窗口（单例）

退出应用 → 退出软件

左键双击托盘图标也可打开设置窗口（与右键菜单中的“设置属性”行为一致）

3.5 多屏幕支持
软件需要检测所有屏幕（主屏+扩展屏），每个屏幕独立分辨率及缩放比例（如2560×1600 @150% 与 1920×1080 @100%）

锁屏时黑色遮罩覆盖所有屏幕

工作倒计时窗口默认显示在 主屏幕 右上角；若用户将窗口拖到扩展屏幕，下次启动时是否记忆位置？需求未明确，建议 每次启动时默认显示在主屏幕右上角（简化处理）。

3.6 全屏应用检测
触发条件：应用全屏运行（FullScreen），特征为：

窗口大小等于屏幕工作区大小

窗口无标准标题栏（或标题栏隐藏）

无最大化、最小化、关闭按钮

常见的全屏场景：PPT演示、浏览器/资源管理器 F11、游戏、视频播放器全屏

处理逻辑：若设置中勾选“软件全屏运行时暂停计时”：

工作时倒计时 暂停

直到没有全屏应用后 恢复计时（继续原有倒计时）

排除：系统应用（如任务管理器、开始菜单、软件自身）不应触发暂停

注意：全屏窗口即使不是当前激活窗口，也应暂停计时。

3.7 键盘鼠标静止检测
检测范围：全局键盘输入、鼠标移动/点击

逻辑：

勾选后，若静止时间 ≥ X 分钟 → 暂停工作倒计时

用户恢复操作（键盘或鼠标任意动作） → 开始一个全新的工作周期（倒计时重置为完整工作时间长度）

不勾选时，此功能不生效。

3.8 单实例运行
确保系统只能同时运行一个程序实例

若尝试再次启动，应激活已有实例（或弹出提示，需求未明确，建议激活已有窗口）

3.9 开机自启动
通过写入注册表或创建启动快捷方式实现

用户可在设置中勾选/取消

3.10 状态持久化
用户关机再开机后，倒计时状态 重新开始，不恢复关机前的剩余时间（符合需求第4条）

4. 音频资源
所有 .mid 文件位于程序运行目录下的 sounds或resources\\sounds\\ 文件夹：

文件名	用途
breakpre.mid	休息前3分钟提醒
break.mid	进入休息时播放
unlock.mid	解锁屏幕时播放
打包时需将 resources\\sounds 整个目录包含在安装包中。

5. 交互与确认框
除“取消”操作外，以下操作需弹出确认框（使用原生 MessageBox 样式）：

立即休息

推迟休息（任意分钟）

保存设置

退出应用

确认框内容示例：“确定立即休息吗？”

6. UI 设计风格
主题：深色主题，科技简洁

字体：清晰易读

工作倒计时窗口：

背景：深灰色半透明或纯黑

文字：白色或亮青色

进度条：亮蓝色或绿色，从左向右消退

锁屏遮罩：纯黑色（#000000），中央倒计时文字白色大号字体

设置窗口：深色背景，控件采用扁平风格，圆角元素可选

7. 技术约束
项目	要求
单实例机制
全局钩子	键盘/鼠标静止检测、全屏应用检测
按键屏蔽	需在锁屏期间拦截低级键盘/系统按键（注意权限）
多屏幕锁定	使用  创建全屏遮罩窗体
8. 异常与边界处理
用户在工作倒计时期间修改了工作时间长度 → 倒计时立即以新时长重新开始

用户修改了休息时间长度 → 下一次休息时生效

音频文件缺失 → 静默跳过，不影响核心功能，可在日志中记录错误

全屏检测误判 → 提供“忽略当前全屏应用”的隐藏机制？非本次需求，可后续迭代

9. 验收标准
工作倒计时窗口正确显示 MM:SS，进度条动态变化。

右键菜单所有功能正常，推迟次数限制生效。

休息时锁定所有屏幕，屏蔽指定系统按键。

三种休息模式行为符合预期。

设置窗口保存配置即时生效，单例限制有效。

全屏应用时暂停/恢复计时正确。

键盘鼠标静止检测后重启新周期。

多屏幕（不同分辨率/缩放）下锁屏遮罩完美覆盖。

托盘图标及右键菜单正常。

单实例运行，二次启动激活已有窗口。

所有确认框正确弹出。

